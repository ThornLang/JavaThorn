// Test basic variables
x = 10;
@immut PI = 3.14159;

// Test expressions
result = x * 2 + 5;

// Test functions
$ greet(name) {
    return "Hello, " + name + "!";
}

greeting = greet("Thorn");

// Test null coalescing
maybe_null = null;
safe_value = maybe_null ?? "default value";

// Test power operator
squared = 2 ** 3;

// Test lists
numbers = [1, 2, 3, 4, 5];
first = numbers[0];

// Test dictionaries
person = {"name": "Alice", "age": "25"};
name = person["name"];