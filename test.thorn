// Test map function from collections module
import "collections";

// Test map function with numbers
numbers = [1, 2, 3, 4, 5];
doubled = map(numbers, $(x) => x * 2);
print("Original: " + numbers);
print("Doubled: " + doubled);

// Test map function with strings (when string methods are available)
words = ["hello", "world", "test"];
// uppercased = map(words, $(s) => s.toUpperCase());
// print("Original: " + words);
// print("Uppercased: " + uppercased);

// Test map function with more complex transformation
squares = map(numbers, $(x) => x * x);
print("Squares: " + squares);

// Test with negative numbers
negatives = [-1, -2, -3];
absolutes = map(negatives, $(x) => x * -1);
print("Negatives: " + negatives);
print("Absolutes: " + absolutes);

print("All map tests completed successfully!");
